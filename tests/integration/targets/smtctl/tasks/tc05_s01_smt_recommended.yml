---
#######################################################
# Setup Tasks
#######################################################
# - name: "SETUP: check if first run"
#   shell: ls /tmp/tc05_s01_smt_recommended || echo "first run"
#   changed_when: false
#   register: run

#######################################################
# Actual Tasks to be Tested
#######################################################
- name: "TC05_S01: sets the number of threads to a 
        value that provides the best performance"
  smtctl:
    smt_extra: "{{ smt_extra }}"
  register: results
- debug: var=results

- name: "TC05_S01_ASSERT: expected results - first/second run"
  assert:
    that:
      - results is changed
      - results.msg.find( "Command Executed Successfully cmd" ) != -1
  # when: run.stdout.find( "first run" ) != -1

# - name: "SETUP: determines completed second run"
#   shell: touch /tmp/tc05_s01_smt_recommended
#   when: run.stdout.find( "first run" ) != -1

# TODO: this action is not idempotent, enable this assert
# after it is decided to fix this minor issue
# - name: "TC04_S01_ASSERT2: expected results - second run"
#   assert:
#     that:
#       - results is not changed
#       - results.msg.find( "No Changes" ) != -1
#   when: run.stdout.find( "/tmp/tc05_s01_smt_recommended" ) != -1

# - name: "SETUP: cleanup environment"
#   shell: rm /tmp/tc02_s01_smt_set_val_nxt_boot
#   when: run.stdout.find( "/tmp/tc05_s01_smt_recommended" ) != -1
